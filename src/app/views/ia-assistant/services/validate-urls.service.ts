import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ValidateUrlsService {
  //This might not need to exist as a service. Maybe all functions belong in component.

}
